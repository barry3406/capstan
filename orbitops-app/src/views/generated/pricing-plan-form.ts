import type { ViewDefinition } from "../../types.js";

export const pricingPlanFormView = {
  "key": "pricingPlanForm",
  "title": "价格计划表单",
  "kind": "form",
  "resource": "pricingPlan",
  "capability": "upsertPricingPlan"
} satisfies ViewDefinition;
