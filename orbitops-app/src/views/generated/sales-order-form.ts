import type { ViewDefinition } from "../../types.js";

export const salesOrderFormView = {
  "key": "salesOrderForm",
  "title": "创建销售订单",
  "kind": "form",
  "resource": "salesOrder",
  "capability": "createSalesOrder"
} satisfies ViewDefinition;
