import type { ViewDefinition } from "../../types.js";

export const salesOrderDetailView = {
  "key": "salesOrderDetail",
  "title": "销售订单详情",
  "kind": "detail",
  "resource": "salesOrder",
  "capability": "activateSalesOrder"
} satisfies ViewDefinition;
