import type { ArtifactDefinition } from "../types.js";

export const customerHealthSnapshotArtifact = {
  "key": "customerHealthSnapshot",
  "title": "客户健康快照",
  "description": "A generated report artifact produced after review completes.",
  "kind": "report"
} satisfies ArtifactDefinition;
