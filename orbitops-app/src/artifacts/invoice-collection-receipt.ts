import type { ArtifactDefinition } from "../types.js";

export const invoiceCollectionReceiptArtifact = {
  "key": "invoiceCollectionReceipt",
  "title": "发票催收回执",
  "description": "A generated record artifact produced after collect completes.",
  "kind": "record"
} satisfies ArtifactDefinition;
