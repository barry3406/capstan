import type { ArtifactDefinition } from "../types.js";

export const refundExecutionReceiptArtifact = {
  "key": "refundExecutionReceipt",
  "title": "退款执行回执",
  "description": "A generated record artifact produced after process completes.",
  "kind": "record"
} satisfies ArtifactDefinition;
