import type { ArtifactDefinition } from "../types.js";

export const renewalRiskDigestArtifact = {
  "key": "renewalRiskDigest",
  "title": "续费风险摘要",
  "description": "A generated report artifact produced after launch completes.",
  "kind": "report"
} satisfies ArtifactDefinition;
