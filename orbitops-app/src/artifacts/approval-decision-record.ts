import type { ArtifactDefinition } from "../types.js";

export const approvalDecisionRecordArtifact = {
  "key": "approvalDecisionRecord",
  "title": "审批决定记录",
  "description": "A generated record artifact produced after decide completes.",
  "kind": "record"
} satisfies ArtifactDefinition;
