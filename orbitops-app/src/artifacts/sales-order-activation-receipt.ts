import type { ArtifactDefinition } from "../types.js";

export const salesOrderActivationReceiptArtifact = {
  "key": "salesOrderActivationReceipt",
  "title": "订单激活回执",
  "description": "A generated record artifact produced after activate completes.",
  "kind": "record"
} satisfies ArtifactDefinition;
