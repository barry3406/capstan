# Operations Console

This project was generated by Capstan from an App Graph.

## Package Name

`operations-app`

## Domain

- Key: `operations`
- Title: Operations Console


## Included Projections

- resource stubs
- generated human surface shell
- generated capability definitions
- user-owned capability handlers
- generated view definitions
- generated application assertions
- user-owned custom assertions
- generated release contract
- generated release environment snapshot
- generated migration plan
- task, policy, artifact, and view registries
- an AI-facing control plane entry point

## Recommended Next Steps

1. Implement the generated capability handlers in `src/capabilities`
2. Open `human-surface.html` to review the generated operator-facing shell
3. Add domain-specific checks in `src/assertions/custom.ts`
4. Replace the placeholder control-plane execution behavior in `src/control-plane/index.ts`
5. Review `capstan.release-env.json` and `capstan.migrations.json` before deployment verification
6. Run `capstan build` to refresh the generated deployment contract and inspect `capstan.release.json`
7. Run `capstan verify --deployment --path . --target docker` (or another target) to validate deployment assumptions before shipping
