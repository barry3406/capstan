{
  "name": "@zauso-ai/capstan-ai",
  "version": "1.0.0-beta.7",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "clean": "rm -rf dist" },
  "description": "AI agent toolkit — think, generate, memory, agent loop. Works standalone or with Capstan.",
  "license": "MIT",
  "author": "barry3406",
  "publishConfig": { "access": "public" }
}
